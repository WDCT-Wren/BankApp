package SaveUp; 

public class PasswordValidator {
    boolean has8Digits = false;
    boolean hasNumber = false;
    boolean hasCapital = false;
    boolean hasSpecialChar = false;

    public boolean validPass(Transactions password) {
        //Checks the password if there are at least 8 digits

        //Checks the password if it contains at least 1 numeric character

        //Checks the password if it contains at least one capital letter

        //Cheks if the password if it contains at least one special character (!@#$%^&*)

        //if all booleans are true, then
        //return the method as true to the transactions class
        return true;
    }
}
